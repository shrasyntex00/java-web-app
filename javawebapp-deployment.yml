#---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: javaapp
  namespace: test-ns
  labels:
    app: nginx
spec:
  replicas: 2
  selector:
    matchLabels:
      app: demo
  template:
    metadata:
      labels:
        app: demo
    spec:
      containers:
      - name: java
        image: account1996/java:1
        ports:
        - containerPort: 80  
---
# Node Port Service
#apiVersion: v1
#kind: Service
#metadata:
#  labels:
#    name: javawebapp
#  name: javawebapp
#spec:
#  type: NodePort
#  ports:
#    - port: 8080
#      targetPort: 8080
#  selector:
#    name: javawebapp
